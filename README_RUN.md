To repo uruchamiaj przez `conda`, nie przez gołe `pip` systemowe.

Skrót:

```bash
conda env create -f environment.yml
conda activate eye-tracking-research
python main.py
```

Pełna dokumentacja jest w `README.md`.
